import Chat from "@/components/Chat";

const page = () => {
  return (
    <div className="flex w-full flex-col container items-center justify-between">
      <Chat />
    </div>
  );
};

export default page;
